Add these sound files in this folder for auction effects:

  bid.mp3   - plays when a new bid arrives
  tick.mp3  - plays during timer countdown (each second)
  hammer.mp3 - plays when a player is sold

The app will work without them; sounds are optional.
